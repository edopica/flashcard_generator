[project]
name = "flashcard-generator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=1.108.2",
    "pdfplumber>=0.11.7",
    "genanki>=0.13.0",
    "requests>=2.32.5",
]

[project.scripts]
flash_gen = "flash_gen:main"
anki_upload = "anki_uploader:main"
